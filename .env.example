# Copy to .env and edit. Everything has a sane default except the password —
# the dashboard runs WITHOUT auth (and warns you) until you set these.

DASH_USER=admin
DASH_PASS=change-me-please

# PORT=8000
# HOST=0.0.0.0
# DB_PATH=analytics.db
